import React from 'react';

export default function LocationData({ ducks }) {
		// ducks.filter((item) => item.locationName)
		// .map((location) => {
		// 	return location;
		// });

	return <div>GetDuckData</div>;
}
